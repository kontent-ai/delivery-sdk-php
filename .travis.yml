language: php

php:
  - 7.0
  - 7.1
  
install:
  - composer self-update
  - composer install

after_success:
  - vendor/bin/test-reporter

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: phpsdk
  github_token: 25bb6fb71e19b65b82bb61cdd25f1184a765925d
  on:
    branch: master
